<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylehome.css">
    <link rel="stylesheet" href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <title>chatset</title>
</head>
<body>
    
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">chatset</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link"  href="#">Men</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Female</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Kids</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Electronics</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Accossories</a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Account
                </a>
                <ul class="dropdown-menu">
                  <li><a class="dropdown-item" href="#">Name :</a></li>
                  <li><a class="dropdown-item" href="#">Ph. No :</a></li>
                  <li><a class="dropdown-item" href="#">E-mail :</a></li>
                </ul>
              </li>
            </ul>
          </div>
        </div>
      </nav>
      <form action="/login">
        <input type="submit" value="Log Out" class="btn btn-danger exit">
      </form>
      <div class="container">
        <div class="products">
          
          
        </div>
      </div>
      <script>
        document.addEventListener('DOMContentLoaded',()=>{
          const fetchData=async(url)=>{
              const data=await fetch(url);
              const realData=await data.json();
              console.log(realData);

            let products=document.querySelector('.products')
            for(let i=0; i<realData.length; i++){
              products.innerHTML+=`<div class="product">
            <img src="${realData[i].images[1]}" alt="" srcset="" class="image">
            <h3 class="title">${realData[i].title}</h3>
            <h5 class="category">${realData[i].category.name}</h5>
            
            <div class="addprize">
              <h4 class="prize">$${realData[i].price}</h4>
              <a href="#" class="addto"></a>
            </div>
          </div>`;
            }
          }
          fetchData("https://api.escuelajs.co/api/v1/products")
        })
      </script>
      <script src="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js">
      </script>
</body>
</html>
